server:
  port: 9999
  logLevel: "debug"
  corsOrigins: "http://localhost:3000"

postgres:
  user: "pguser"
  password: "pgpass"
  host: "pg"
  port: 5432
  db: "dev"
  ssl: false

frameService:
  host: "frame"
  port: 10000

minio:
  host: "minio"
  accessKey: "accessKey"
  secretKey: "secretKey"
  ssl: false
